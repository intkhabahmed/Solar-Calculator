<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/search_place"
        android:icon="@drawable/ic_search_black_24dp"
        android:title="@string/search"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/save_pin"
        android:icon="@drawable/ic_pin_drop_black_24dp"
        android:title="@string/save_pin"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/saved_pins"
        android:icon="@drawable/ic_bookmark_black_24dp"
        android:title="@string/saved_pins"
        app:showAsAction="ifRoom"/>

</menu>